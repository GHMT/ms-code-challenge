export default interface ISys {
    type: string;
    id: string;	
    // space:  Link;
    // contentType: Link;
    revision: number;	
    createdAt: Date;	
    updatedAt: Date;
    locale: string;	
}